<!DOCTYPE html>
<html lang="en">

<%- include('../partials/header.ejs') %>

<body>
    <h2>Your TASTIES List</h2>
    <h3>Viewing Results for <%=user%></h3>

    <div>
        <% for(restaurant of restaurants) { %>
            <article>
                <a href="/restaurants/<%=restaurant._id%>"><h2><%=restaurant.name%></h2></a>
                <!-- Aubrey look at this because you did it wrong the first time -->
            </article>
        <% } %>
    </div>
    
    <br>
    <a href="/restaurants/new"><button>Add a Restaurant</button></a>
</body>
</html>
